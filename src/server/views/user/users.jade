extends ../layout
block vars
    -currentPage = "Users";
block content
    div.content
        h1 #{locals.user.username}
        span User ID: #{locals.user._id}
        br
        span User auth level: #{locals.user.userRights}