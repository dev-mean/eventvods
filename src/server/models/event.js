function Event (format, beginDate, endDate, tournamentLocation, casters, panel) {
	this.format = format;
	this.beginDate = beginDate;
	this.endDate = endDate;
	this.tournamentLocation = tournamentLocation;
	this.caster = caster;
	this.panel = panel;
}